import React from 'react';

const QuranPage = ({ page }) => {
  // هنا يمكنك إضافة نص الصفحة من القرآن الكريم
  return (
    <div>
      <h2>الصفحة {page}</h2>
      {/* عرض نص الصفحة هنا */}
    </div>
  );
};

export default QuranPage;
